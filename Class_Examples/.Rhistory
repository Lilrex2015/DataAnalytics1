for (i in 1:3)
{
print(i)
}
1:10
2*(1:10)
z.vec <- numeric(0)
z<- 1
for(i in 1:5)
{
z <- 2*z
z.vec[i]
}
print(z.vec)
z.vec <- numeric(0)
z<- 1
for(i in 1:5)
{
z <- 2*z
z.vec[i]
}
print(z.vec)
install.packages('ggplot2')
qplot(
x = (1:100), y=(1:100)^2, xlab='x', ylab='y'
)
qplot(x = (1:100), y=(1:100)^2, xlab='x', ylab='y')
library(ggplot2)
qplot(x = (1:100), y=(1:100)^2, xlab='x', ylab='y')
install.packages('quantmode')
install.packages('quantmod')
library(quantmod)
getSymbols(Symbols = 'TLSA', src = 'yahoo', from="2022-09-10")
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10")
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10")
View(TLSA)
getSymbols(Symbols = 'V', src = 'yahoo', from="2022-09-10")
View(V)
TSLA.data <- data.frame(ticker.date = as.DAte(index(TSLA)),
closing.price = as.numeric())
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10" env=NULL)
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10" env=NULL)
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10", env=NULL)
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-10")
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
index(TSLA)
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2020-09-10")
#env = null just makes it not an Object
index(TSLA)
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-01")
#env = null just makes it not an Object
index(TSLA)
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
closing.price = as.numeric(TSLA$TSLA.close))
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
Closing.price = as.numeric(TSLA$TSLA.close))
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
Closing.price = as.numeric(TSLA$TSLA.close))
getSymbols(Symbols = 'TSLA', src = 'yahoo', from="2022-09-01")
View(TSLA)
index(TSLA)
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),
Closing.price = as.numeric(TSLA$TSLA.close))
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),Closing.price = as.numeric(TSLA$TSLA.close))
as.numeric(TSLA$TSLA.close)
as.Date(index(TSLA))
TSLA.data <- data.frame(ticker.date = as.Date(index(TSLA)),Closing.price = as.numeric(TSLA$TSLA.Close))
load('HW1_data2022.rData')
gg <- BTC.charts$high
print(gg)
